# test/fixtures/users.yml

user_andre:
  email: andre@wawa.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

user_bjarke:
  email: bjarke@wawa.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

user_cecilie:
  email: cecilie@wawa.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
