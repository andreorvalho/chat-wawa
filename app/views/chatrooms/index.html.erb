<% @chatrooms.each do |chatroom| %>
  <ul class="list-group">
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <%= chatroom.name %>
      <%= link_to "Join", chatroom_path(chatroom), class: "btn btn-secondary" %>
    </li>

  </ul>
<% end %>
